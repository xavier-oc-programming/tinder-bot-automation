# Python 3.8+
selenium
python-dotenv

# ChromeDriver must be installed separately and match your Chrome version.
# macOS (Homebrew):  brew install chromedriver
# Or download from: https://chromedriver.chromium.org/downloads
